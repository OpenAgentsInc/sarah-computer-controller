---
"sarah-computer-controller": minor
---

Curated tier gains a named execute allowlist (`curatedExecute`, default `["gh"]`): a delegated ACP agent may run commands whose every chain segment heads with an allowlisted program (or `cd`). Owner opt-in so agents can post the GitHub issues they draft.
